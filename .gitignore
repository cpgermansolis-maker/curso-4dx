# Dependencias de Node
node_modules/
package-lock.json
# Nota: package.json a nivel raíz se ignora, pero functions/package.json SÍ se incluye
/package.json

# Material fuente de libros (copyright, no se publican). Repo PÚBLICO →
# se ignora la carpeta completa para que ningún libro nuevo se cuele por error.
# (libros/ también está excluido del hosting en firebase.json → hosting.ignore.)
libros/

# Notas locales de asistentes (Copilot, etc.) — sin valor para el repo público
RESUMEN-CONTEXTO-COPILOT.md

# Archivos temporales
tmp-*.js
tmp-*.txt
tmp-*.json
tmp-*.md
tmp-feum-texts/
*.tmp
*.log

# Archivos del sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Claude Code
.claude/

# Firebase CLI cache local
.firebase/

# Grafo de análisis (graphify): artefacto regenerable con /graphify.
# Se ignora porque (a) se reconstruye desde el código, (b) son MB de JSON/HTML
# que ensucian diffs e historial, y (c) graphify-out/converted/ contiene markdown
# derivado de los libros fuente protegidos (FEUM, etc.) — y este repo es PÚBLICO.
graphify-out/
